---
galaxy_info:
  author: Lenny Shirley
  description: Render a CSV of Controller jobs from controller_jobs_raw (no extra API calls).
  license: MIT
  min_ansible_version: "2.15.0"
  galaxy_tags:
    - aap
    - controller
    - jobs
    - reports
dependencies:
  - role: global_vars
