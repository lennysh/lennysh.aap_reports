---
galaxy_info:
  author: Lenny Shirley
  description: Fetch all AAP Controller organizations (paginated). Sets controller_organizations. Requires controller_detect (controller_api_base_path).
  license: MIT
  min_ansible_version: "2.15.0"
  galaxy_tags:
    - aap
    - controller
    - api
dependencies:
  - role: global_vars
...
