---
# Fetch all organizations (paginated). Recursively includes self for next page.
# Dumps into controller_organizations (list). Uses controller_api_base_path.

- name: Fetch organizations page {{ controller_fetch_page | default(1) }}
  ansible.builtin.uri:
    url: "{{ aap_url }}{{ controller_api_base_path }}/organizations/?page={{ controller_fetch_page | default(1) }}&page_size={{ controller_page_size }}"
    method: GET
    user: "{{ aap_username if (aap_token is not defined or aap_token | length == 0) else omit }}"
    password: "{{ aap_password if (aap_token is not defined or aap_token | length == 0) else omit }}"
    force_basic_auth: "{{ (aap_token is not defined or aap_token | length == 0) and (aap_username is defined and aap_username | length > 0) }}"
    headers: "{{ {'Authorization': 'Bearer ' ~ aap_token} if (aap_token is defined and aap_token | length > 0) else omit }}"
    validate_certs: "{{ aap_validate_certs | default(true) | bool }}"
    return_content: true
    status_code: [200]
  register: controller_org_page

- name: Append organizations page to controller_organizations
  ansible.builtin.set_fact:
    controller_organizations: "{{ (controller_organizations | default([])) + (controller_org_page.json.results | default([])) }}"

- name: Brief pause between API requests
  ansible.builtin.pause:
    seconds: "{{ controller_request_delay }}"
    prompt: ""
  when: (controller_org_page.json.next | default('', true)) | length > 0

- name: Set next organizations page number
  ansible.builtin.set_fact:
    _controller_next_page: "{{ (controller_fetch_page | default(1)) | int + 1 }}"
  when: (controller_org_page.json.next | default('', true)) | length > 0

- name: Fetch next organizations page
  ansible.builtin.include_tasks: fetch_organizations.yml
  vars:
    controller_fetch_page: "{{ _controller_next_page }}"
  when: (controller_org_page.json.next | default('', true)) | length > 0
...
