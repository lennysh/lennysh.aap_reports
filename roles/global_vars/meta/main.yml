---
galaxy_info:
  author: Lenny Shirley
  description: Shared default variables for AAP Controller API usage (pagination, request delay). No tasks; include as dependency so controller_* roles get consistent vars.
  license: MIT
  min_ansible_version: "2.15.0"
  galaxy_tags:
    - aap
    - controller
    - vars
dependencies: []
...
